services:
  mongo:
    image: mongo:7
    container_name: control-finance-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: control_finance
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
